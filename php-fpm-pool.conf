[www]
user = www-data
group = www-data

clear_env = no

; Keep HOME outside /root so libpq can resolve the default cert path
env[HOME] = /var/www

pm = static
pm.max_children = 2

listen = 127.0.0.1:9000